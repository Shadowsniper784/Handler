module.exports = (client, instance) => {
 // console.log('Client: %o\nInstance: %o', client, instance)
  //console.log('e')
  client.on('message', (message) => {
   // instance.CommandHandler.runCommand(args, message, instance)
  })
}